name: quantum_list
description: A revolutionary list widget for Flutter with atomic rendering, advanced animations, and powerful controllers
version: 1.3.0
homepage: https://github.com/ShoghShahadat/QuantumList

environment:
  sdk: '>=2.18.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# The following section is specific to Flutter packages.
# flutter:
  # این بخش برای زمانی است که بخواهید فونت یا تصویری را به پکیج اضافه کنید.
  # This section is for adding assets or fonts to your package.
