name: quantum_list
description: یک ویجت لیست انقلابی برای فلاتر با رندرینگ اتمی، انیمیشن‌های پیشرفته و کنترلرهای قدرتمند.
version: 1.3.0
# homepage: # آدرس گیت‌هاب شما در اینجا قرار می‌گیرد

environment:
  sdk: '>=2.18.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# The following section is specific to Flutter packages.
# flutter:
  # این بخش برای زمانی است که بخواهید فونت یا تصویری را به پکیج اضافه کنید.
  # This section is for adding assets or fonts to your package.
