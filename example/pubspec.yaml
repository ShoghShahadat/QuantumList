name: example
description: A showcase of the revolutionary QuantumList package.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # وابستگی به پکیج اصلی که در پوشه قبلی قرار دارد
  quantum_list:
    path: ../

  # وابستگی برای انتخاب رنگ
  flutter_colorpicker: ^1.0.3

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
